import * as moment from 'moment';
export var MultiPickerUtils = (function () {
    function MultiPickerUtils() {
    }
    MultiPickerUtils.minuteRound = function (val, rounding) {
        if (rounding == 1)
            return moment(val);
        else {
            var _moment = moment(val);
            var minuteTail = _moment.minute() % rounding;
            return _moment.subtract(minuteTail, 'minutes').set('second', 0);
        }
    };
    return MultiPickerUtils;
}());
//# sourceMappingURL=util.js.map