var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
import { MultiPickerColumn } from '../multi-picker-columns';
export var MultiPickerColumnNoon = (function (_super) {
    __extends(MultiPickerColumnNoon, _super);
    function MultiPickerColumnNoon() {
        _super.apply(this, arguments);
        this.name = 'noon';
        this.firstOptionValue = this.min.hour() > 11 ? 1 : 0;
        this.lastOptionValue = this.max.hour() > 11 ? 1 : 0;
    }
    MultiPickerColumnNoon.prototype.optionText = function (num) {
        return this.format.noons[num];
    };
    return MultiPickerColumnNoon;
}(MultiPickerColumn));
//# sourceMappingURL=noon.js.map