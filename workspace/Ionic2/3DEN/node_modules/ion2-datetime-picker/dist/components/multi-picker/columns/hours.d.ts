import { MultiPickerColumn, IMultiPickerColumn } from '../multi-picker-columns';
export declare class MultiPickerColumnHours extends MultiPickerColumn implements IMultiPickerColumn {
    minHour: number;
    maxHour: number;
    existingHours: Object;
    name: string;
    firstOptionValue: number;
    lastOptionValue: number;
    filter(noon: number): Array<number>;
    filterMeridiem(noon: any): MultiPickerColumnHours;
    protected optionText(num: number): string;
}
