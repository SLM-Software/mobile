var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
import * as moment from 'moment';
import { MultiPicker } from '../multi-picker';
import { MultiPickerColumn } from '../multi-picker-columns';
var currentYear = moment().year();
export var MultiPickerColumnYears = (function (_super) {
    __extends(MultiPickerColumnYears, _super);
    function MultiPickerColumnYears() {
        _super.apply(this, arguments);
        this.name = 'years';
        this.firstOptionValue = currentYear - MultiPicker.YEAR_ROUND;
        this.lastOptionValue = currentYear + MultiPicker.YEAR_ROUND;
    }
    return MultiPickerColumnYears;
}(MultiPickerColumn));
//# sourceMappingURL=years.js.map