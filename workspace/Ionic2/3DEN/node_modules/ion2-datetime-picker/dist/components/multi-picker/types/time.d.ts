import { PickerColumn } from 'ionic-angular';
import { MultiPickerType, IMultiPickerTypeTimeColumns, IMomentObject } from '../multi-picker-types';
export declare class MultiPickerTypeTime extends MultiPickerType {
    protected _columns: IMultiPickerTypeTimeColumns;
    private min;
    private max;
    private minuteRounding;
    private format;
    constructor(cmpAttrs: any);
    validate(columns: PickerColumn[], pickerValue?: string): void;
    dealDoneVisibleBnt(columns: PickerColumn[], button: any): void;
    protected defaultMoment(pickerValue: string): IMomentObject;
    private parseFormat(pickerFormat);
}
