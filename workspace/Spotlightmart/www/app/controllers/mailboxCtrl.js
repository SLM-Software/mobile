SpotlightmartApp.controller('mailboxCtrl', function ($scope, CordovaService, $cordovaFile, $location, $rootScope) {
    CordovaService.ready.then(function () {
        init();

        function init() {
        }

    });
});